[project]
name = "profesor-ai"
version = "1.0.0"
description = "El Profesor Mindset Trainer with Web Intel Agent"
requires-python = ">=3.9"
dependencies = []

[tool.vercel]
entrypoint = "api.chat:app"
